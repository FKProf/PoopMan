# Copilot Instructions

## Project Guidelines
- In this codebase, `Region` is defined in `PoopManLibrary\Graphics\Sprite.cs`, so `AnimatedSprite` should use that existing member instead of introducing a duplicate.